import 'package:flutter/material.dart';

class AppColors {
  static const appBarForegroundColor = Colors.black;
  static const appBarColor = Colors.transparent;
  static const iconColor = Colors.purple;
  static const verifiedIconColor = Colors.green;
  static const errorColor = Colors.red;
  static var scaffoldBackgroundColor = Colors.blueGrey[50];
}
